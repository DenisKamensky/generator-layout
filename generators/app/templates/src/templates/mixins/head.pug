mixin head(titleName, styleFileName)
  head
    title=titleName
    meta(charset='UTF-8')
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet", href=`styles/${styleFileName}.css`)
